[package]
name = "gwi-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Git With It — bare clone / fetch CLI (Phase 0 stub)"

[[bin]]
name = "gwi-git"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
url = "2.5"
